============
Installation
============

At the command line::

    $ easy_install django_activitylog

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_activitylog
    $ pip install django_activitylog
